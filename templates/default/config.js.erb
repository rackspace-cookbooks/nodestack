var config = {};

config = {
  <% @config_js.each_pair do | directive, config | %>
    <% if config.is_a?(Integer) %>
        <%= directive %>: <%= config %>,
    <% else %>
        <%= directive %>: "<%= config %>",
    <% end %>
  <% end %>
};

module.exports = config;
