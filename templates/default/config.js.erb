var config = {};

config.node = {
  port: <%= @http_port %>
};

config.mysql = [
  {
    host: "<%= @mysql_ip %>",
    usr: "<%= @mysql_user %>",
    pwd: "<%= @mysql_password %>",
    db_name: "<%= @mysql_db_name %>",
  }

];

module.exports = config;
